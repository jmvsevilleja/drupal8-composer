<?php

/**
 * @file
 * Install, update and uninstall functions for the module.
 */

use Drupal\drupal8_profile_content\InstallHelper;

/**
 * Implements hook_install().
 */
function drupal8_profile_content_install() {
  if (!\Drupal::service('config.installer')->isSyncing()) {
    \Drupal::classResolver(InstallHelper::class)->importContent();
  }
}

/**
 * Implements hook_uninstall().
 */
function drupal8_profile_content_uninstall() {
  if (!\Drupal::service('config.installer')->isSyncing()) {
    \Drupal::classResolver(InstallHelper::class)->deleteImportedContent();
  }
}
